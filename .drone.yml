kind: pipeline
name: default

steps:
- name: build
  image: runmymind/docker-android-sdk
  volumes:
  - name: gradle
    path: /root/.gradle
  commands:
  - ./gradlew :oem-push-android:clean :oem-push-android:build
  - ./gradlew :oem-push-baidu-android:clean :oem-push-baidu-android:build
  - ./gradlew :app:clean :app:build

#- name: snapshot
#  image: runmymind/docker-android-sdk
#  volumes:
#  - name: gradle
#    path: /root/.gradle
#  environment:
#    PUBLISH_TO_JFROG: true
#    BINTRAY_USER:
#      from_secret: BINTRAY_USER
#    BINTRAY_API_KEY:
#      from_secret: BINTRAY_API_KEY
#    VERSION_CODE: ${DRONE_BUILD_NUMBER}
#    VERSION_NAME: 0.0.1-SNAPSHOT
#  commands:
#  - ./gradlew :oem-push-android:clean :oem-push-android:install :oem-push-android:artifactoryPublish
#  - ./gradlew :oem-push-baidu-android:clean :oem-push-baidu-android:install :oem-push-baidu-android:artifactoryPublish
#  when:
#    event:
#    - push

- name: release
  image: runmymind/docker-android-sdk
  volumes:
  - name: gradle
    path: /root/.gradle
  environment:
    PUBLISH_TO_JCENTER: true
    BINTRAY_USER:
      from_secret: BINTRAY_USER
    BINTRAY_API_KEY:
      from_secret: BINTRAY_API_KEY
    VERSION_CODE: ${DRONE_BUILD_NUMBER}
    VERSION_NAME: 0.0.1
  commands:
  - ./gradlew :oem-push-android:clean :oem-push-android:install :oem-push-android:bintrayUpload
  - ./gradlew :oem-push-baidu-android:clean :oem-push-baidu-android:install :oem-push-baidu-android:bintrayUpload
  when:
    event:
    - tag

volumes:
- name: gradle
  temp: {}